{
  "font_download": {
    "description": "字体资源下载",
    "type": "object",
    "hint": "只控制默认字体包的异步下载；下载会继承 AstrBot 全局代理，并在失败时自动切换内置 GitHub 镜像",
    "items": {
      "auto_download": {
        "description": "默认字体缺失时后台自动下载",
        "type": "bool",
        "default": true
      }
    }
  }
}

{
  "corner_radius": {
    "description": "气泡圆角大小",
    "type": "int",
    "default": 27,
    "hint": "例如：27"
  },
  "bubble_font_path": {
    "description": "气泡字体路径；留空使用自动下载的默认字体",
    "type": "string",
    "default": "",
    "hint": "可选：自定义字体的绝对路径"
  },
  "nickname_font_path": {
    "description": "名称字体路径；留空使用自动下载的默认字体",
    "type": "string",
    "default": "",
    "hint": "可选：自定义字体的绝对路径"
  },
  "title_font_path": {
    "description": "头衔字体路径；留空使用自动下载的默认字体",
    "type": "string",
    "default": "",
    "hint": "可选：自定义字体的绝对路径"
  },
  "font_auto_download": {
    "description": "默认字体缺失时后台自动下载",
    "type": "bool",
    "default": true
  },
  "font_github_mirror": {
    "description": "可选 GitHub 镜像前缀；留空直连官方 Release",
    "type": "string",
    "default": "",
    "hint": "使用与 AstrBot 更新器相同的 URL 前缀格式"
  }
}
